# Reverse proxy: same-origin routing so the browser hits /api on the same host
# (no CORS needed in production). Replace :80 with your domain for automatic TLS.
:80 {
	handle /api/* {
		reverse_proxy api:8000
	}
	handle {
		reverse_proxy web:3000
	}
}
